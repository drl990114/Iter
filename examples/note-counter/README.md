# Note Counter
A small local Python 3.10+ CLI: `python3 count_notes.py <directory>` counts only immediate `.md` files. No dependencies, network or real user data. This is a disposable evaluation fixture.
